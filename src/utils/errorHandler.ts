// import { Response } from "express";

// export const handleInternalServerError = (error: Error, res: Response, message: string) => {
//   console.error(`${message}:`, error);
//   res.status(500).json({ message: "Internal server error" });
// };
